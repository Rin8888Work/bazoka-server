{
  "version": 2,
  "builds": [
    {
      "src": "server.js",
      "use": "@vercel/node"
    }
  ],
  "buildCommand": "rm -rf ./temp && git clone https://github.com/Rin8888Work/bazoka-server.git ./temp && cd ./temp && npm install && mv ./* ../ && cd .. && rm -rf ./temp",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/server.js"
    }
  ]
}
